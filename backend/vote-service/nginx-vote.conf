# 投票系统 Nginx 配置
# 使用方法: 复制到服务器的 /etc/nginx/conf.d/vote.conf

server {
    listen 80;
    server_name tp.dzwww.com;  # 修改为你的域名

    # 前端静态文件
    location / {
        root /opt/h5/dist;
        try_files $uri $uri/ /index.html;
        index index.html;

        # 启用 gzip 压缩
        gzip on;
        gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
        gzip_min_length 1024;
        gzip_comp_level 6;

        # 缓存设置
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    # 后端 API 代理
    location /api {
        proxy_pass http://localhost:8082;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 上传文件目录
    location /uploads {
        alias /opt/vote-service/uploads;
        expires 30d;
        add_header Cache-Control "public, immutable";

        # 安全设置
        location ~* \.(php|jsp|cgi|pl|py|sh)$ {
            deny all;
        }
    }

    # 健康检查
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
}

# HTTPS 配置 (使用 SSL 证书后启用)
# server {
#     listen 443 ssl http2;
#     server_name tp.dzwww.com;
#
#     # SSL 证书配置
#     ssl_certificate /etc/letsencrypt/live/tp.dzwww.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/tp.dzwww.com/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     # 其他配置同上...
# }
