# 开发环境配置 - 禁用Nacos
spring:
  config:
    import: "optional:nacos:"
  cloud:
    nacos:
      config:
        enabled: false
        import-check:
          enabled: false
      discovery:
        enabled: false

  # 开发环境数据库配置
  datasource:
    url: jdbc:mysql://localhost:3306/dzvote_new?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&useSSL=false
    username: dzvote
    password: dzvote123
  
  # 开发环境Redis配置
  data:
    redis:
      host: localhost
      port: 6379
      database: 0

logging:
  level:
    com.dzvote: debug
    org.springframework.cloud: warn
    com.alibaba.nacos: warn